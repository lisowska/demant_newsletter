/**
 * Simulated newsletter signup — replace with real API when available.
 */

export function submitNewsletterSignup(): Promise<void> {
  return new Promise((resolve) => {
    window.setTimeout(() => {
      resolve();
    }, 700);
  });
}
